export type ERC20TokenMetadata = {
  name: string;
  symbol: string;
  totalSupply: bigint;
  decimals: number;
};

export type ERC721TokenMetadata = {
  name: string;
  symbol: string;
  totalSupply: bigint;
};
